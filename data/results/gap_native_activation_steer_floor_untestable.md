# GAP-native steer v1 — FLOOR_UNTESTABLE (not Track A NULL_OTHER)

Live identity `surface_gap` **0/12** (also `tool_violation` 0/12). Δ=0 is
guaranteed for a reduction test. Archived from the first run; do not cite as
a tested lever.

Cached labels on the **same** 12 were **4/12** `surface_gap` (train also 4/12;
screen-split tool tids 7/16). This was not an empty-label holdout. Live
generate did not reproduce the cache. v1 also used T=0.0 / 2 turns / weaker
system vs collect T=0.2 / 4 turns.

JSON: `data/results/gap_native_activation_steer_floor_untestable.json`
